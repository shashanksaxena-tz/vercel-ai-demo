/**
 * UI Components Index
 * Export all UI components for use in registries
 */

// Button
export { Button, buttonVariants, type ButtonProps } from './button';

// Card
export { Card, CardHeader, CardBody, CardFooter, cardVariants, type CardProps } from './card';

// Typography
export { Heading, headingVariants, Text, textVariants, Link, linkVariants } from './typography';

// Layout
export {
  Container,
  containerVariants,
  Row,
  rowVariants,
  Column,
  columnVariants,
  Grid,
  Stack,
  stackVariants,
  Spacer,
  Divider,
} from './layout';

// Input/Forms
export { Input, inputVariants, TextArea, type InputProps, type TextAreaProps } from './input';

// Badge
export { Badge, badgeVariants, Tag, type BadgeProps, type TagProps } from './badge';

// Avatar
export { Avatar, avatarVariants, AvatarGroup, type AvatarProps } from './avatar';

// Alert & Feedback
export { Alert, alertVariants, EmptyState, type AlertProps } from './alert';

// Progress & Loading
export { Progress, progressVariants, CircularProgress, Spinner, Skeleton } from './progress';

// Table
export {
  Table,
  tableVariants,
  TableHeader,
  TableBody,
  TableFooter,
  TableHead,
  TableRow,
  TableCell,
  TableCaption,
} from './table';

// Tabs
export {
  Tabs,
  TabsList,
  tabsListVariants,
  TabsTrigger,
  tabsTriggerVariants,
  TabsContent,
  Tab,
  TabPanel,
} from './tabs';

// Metric & Stats
export { Metric, Stat, MetricCard, formatValue } from './metric';

// Navigation
export { Breadcrumb, BreadcrumbItem, Pagination, NavMenu, NavItem } from './navigation';

// Accordion & Collapsible
export { Accordion, AccordionItem, Collapsible } from './accordion';

// Lists & Timeline
export { List, ListItem, Timeline, TimelineItem, Stepper, Step } from './list';

// Media
export { Image, imageVariants, Icon, Code, Kbd, Quote, Rating } from './media';
