services:
  - type: web
    name: ai-ui-playground
    env: node
    rootDirectory: ai-ui-playground
    buildCommand: npm install && npm run build
    startCommand: npm start
    envVars:
      - key: GOOGLE_GENERATIVE_AI_API_KEY
        sync: false
      - key: NODE_VERSION
        value: 20.10.0
